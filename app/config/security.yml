security:

    providers:
        hc_userprovider:
            id: hc_usertools.login_user_provider

        in_memory:
            memory: ~

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        user:
            pattern: ^/user
            provider: hc_userprovider
            hc_user: true

        index:
            pattern: ^/
            anonymous: ~
